# Simple String Replacer for Brunch
Replace a String (as a Regex) with another String everywhere.

In brunch-config.coffee:

...
  plugins:
    replacer:
      dict: [
    	{
    		key: 'search string with regex flavor\\.'
    		value: 'replacement string'
    	}
    ]