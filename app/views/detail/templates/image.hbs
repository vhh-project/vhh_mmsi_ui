<div class="row">
  <div class="col-12">
    {{#if imageUrl}}
      <img src="{{imageUrl}}" />
    {{else}}
      <div class="alert alert-secondary">{{_ 'message.no_image_found'}}</div>
    {{/if}}
  </div>
</div>  